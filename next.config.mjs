/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // Will use ESLint 9 when Next.js officially supports it
    // ignoreDuringBuilds: true,
  },
};

export default nextConfig;
